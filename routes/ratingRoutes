const express = require('express');
const mw = require('../middleware')
const {giveRating} = require('../handlers/ratings');
const router = express.Router();

router.post('/id=:id', mw.isLoggedIn, giveRating);

module.exports = router;